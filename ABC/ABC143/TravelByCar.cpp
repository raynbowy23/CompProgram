#include<iostream>
#include <bits/stdc++.h>
#include <vector>
#define rep(i, n) for (int i=0;i<n;i++);
using namespace std;
typedef long long ll;

int main(){
    cin.tie(0);
    ios::sync_with_stdio(false);

    int x;
    cin >> x;
    cout << x << endl;
}